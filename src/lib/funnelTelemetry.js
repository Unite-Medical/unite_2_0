let memoryId;
export function funnelSession(){try{let id=sessionStorage.getItem('unite.quote.session');if(!id){id=crypto.randomUUID();sessionStorage.setItem('unite.quote.session',id);}return id;}catch{return memoryId ||= crypto.randomUUID();}}
export function trackFunnel(event,metadata={}){if(typeof window==='undefined'||navigator.doNotTrack==='1')return;try{const source=new URLSearchParams(window.location.search).get('utm_source');if(source&&/^[a-z0-9 _-]{1,80}$/i.test(source))sessionStorage.setItem('unite.quote.source',source);metadata={source:sessionStorage.getItem('unite.quote.source')||'direct',...metadata};}catch{/* storage unavailable */}void fetch('/api/telemetry/funnel',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({session_id:funnelSession(),event,metadata}),keepalive:true}).catch(()=>{});}
